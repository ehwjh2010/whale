# 2026-07-16

## LSP 功能代码审查（分支 feat/LSP）
对 `internal/lsp/*` + `internal/tools/catalog_lsp.go` + 应用集成做了纯审查（未改代码）。
- 构建/ vet 通过。`internal/lsp` 无单测（仅 tools 层有 948 行 mock 测试）。
- 两个 P0 必改：① `Manager.Close()` 从未被应用层调用 → LSP 子进程退出时不优雅关闭（泄漏/僵死）；② jsonrpc `readLoop` 把带 ID 的消息一律当响应，不支持服务器主动请求（workspace/configuration 等）。
- P1：manager.go `clientForServer` 删除—重建竞态会泄漏孤儿进程；client.go `ensureDocumentOpen` 内容陈旧且可能重复 didOpen；`Start` 无自带握手超时（依赖调用方 ctx）；崩溃无限重启无熔断。
- 报告写入 `D:\src\whale_lsp\LSP_REVIEW.md`。
